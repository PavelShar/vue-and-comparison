
export const base = {
    title: {
        type: String,
        default: null
    },

    hasHint: {
        type: Boolean,
        default: false
    }
};

export const component = {

    title: {
        type: String,
        default: null
    },

    hint: {
        type: String,
        default: null,
    },

    hoverable: {
        type: Boolean,
        default: true,
    }
};